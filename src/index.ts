// consentium — a lightweight, GDPR-minded cookie-consent toolkit for React.
//
// The root entry re-exports everything. For a framework-agnostic integration
// without React, import from `consentium/core` instead. Remember to import the
// stylesheet once: `import "consentium/styles.css"`.

export * from "./core/index";
export * from "./react/index";
