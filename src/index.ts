// consent-kit — a lightweight, GDPR-minded cookie-consent toolkit for React.
//
// The root entry re-exports everything. For a framework-agnostic integration
// without React, import from `consent-kit/core` instead. Remember to import the
// stylesheet once: `import "consent-kit/styles.css"`.

export * from "./core/index";
export * from "./react/index";
